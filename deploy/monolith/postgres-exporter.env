# /etc/default/prometheus-postgres-exporter
# apt install prometheus-postgres-exporter ile kurulur
# Servis User=prometheus ile çalışır — Unix socket üzerinden bağlanır

DATA_SOURCE_NAME='user=prometheus host=/run/postgresql dbname=postgres'
ARGS=""
