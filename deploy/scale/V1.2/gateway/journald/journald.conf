# /etc/systemd/journald.conf.d/99-teqlif.conf — gateway
# Uygulama: sudo cp bu_dosya /etc/systemd/journald.conf.d/99-teqlif.conf && sudo systemctl restart systemd-journald
# gateway — 60 GB SSD, observability node

[Journal]
SystemMaxUse=300M
SystemKeepFree=1G
