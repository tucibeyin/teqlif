# gateway — Production
# Kullanım: alertmanager.service EnvironmentFile olarak okur (--config.expand-env ile alertmanager.yml'e inject edilir)
# Doldurma: gerçek değerleri girdikten sonra chmod 600 uygula

# Telegram bildirimleri — alertmanager.yml ${} ile okur
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
