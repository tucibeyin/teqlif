# node5 (Core) — Production

DATABASE_URL=

REDIS_URL="redis://:<CORE_REDIS_PASSWORD>@10.10.0.5:6379/0"
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200

UPLOAD_DIR=/var/www/teqlif.com/uploads

# E-posta (Brevo)
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=

# --- V1.4 EDGE MİMARİSİ (Dinamik Medya & Yayın) ---
# Edge (Node1, Node4 vb.) adresleri aralarında virgül bırakılarak listelenir.
# Orchestrator servis bunları dinamik olarak Redis CPU/Disk metriklerine göre seçer.
EDGE_LIVEKIT_URLS="http://10.10.0.1:7880,http://10.10.0.6:7880"
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

EDGE_MINIO_URLS="http://10.10.0.1:9010,http://10.10.0.6:9010"
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=teqlif
MINIO_DM_BUCKET=teqlif-dm
MINIO_SECURE=False
MINIO_REGION=us-east-1
# MinIO'nun edge disklerinde kullanabileceği maksimum kota (Yüzde)
MINIO_STORAGE_QUOTA_PERCENT=80
# Edge'lerin metrik bildirim sıklığı
EDGE_METRICS_INTERVAL_SEC=3

# --- V1.4 AI PROXY MİMARİSİ (Groq & Gemini Bypass) ---
# Core, doğrudan yapay zekaya gitmez; istekleri bu iki proxy'ye dağıtır.
NODE2_AI_PROXY_URL="http://10.10.0.3:8080"
NODE3_AI_PROXY_URL="http://10.10.0.4:8080"
AI_PROXY_INTERNAL_TOKEN=

# Firebase (FCM push)
FIREBASE_SERVICE_ACCOUNT=

# Sentry
SENTRY_BACKEND_DSN=

# Google OAuth
GOOGLE_CLIENT_ID=

# Admin
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=

# Captcha
CAPTCHA_ENABLED=
CAPTCHA_PROVIDER=
CAPTCHA_SECRET_KEY=

# DB bağlantı havuzu
DB_POOL_SIZE=
DB_MAX_OVERFLOW=
DB_POOL_TIMEOUT=
DB_POOL_RECYCLE=

# Telegram bildirim
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Apple Push Notifications (APNS - VoIP)
APNS_KEY_PATH=
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_CERT_PATH=
IOS_BUNDLE_ID=teqlif
APNS_USE_SANDBOX=False

# Genel Uygulama Ayarları
SITE_URL=https://www.teqlif.com
DEBUG=False
WEB_APP_ENABLED=False
USE_PGBOUNCER=False

# ClickHouse Analytics
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DB=teqlif_prod_analytics

# Backup (teqlif-backup.sh tarafından okunur)
# BACKUP_REMOTE_HOST: yedeklerin rsync ile gönderileceği node WireGuard IP'si
# BACKUP_REMOTE_USER: SSH kullanıcısı (varsayılan: tucibeyin)
# BACKUP_REMOTE_PATH: uzak backup kök dizini
# BACKUP_RETENTION_LOCAL_DAYS: bu node'da kaç gün tutulur (varsayılan: 2)
# BACKUP_RETENTION_REMOTE_DAYS: uzak node'da kaç gün tutulur (varsayılan: 7)
BACKUP_REMOTE_HOST=10.10.0.4
BACKUP_REMOTE_USER=tucibeyin
BACKUP_REMOTE_PATH=/var/backups/teqlif
BACKUP_RETENTION_LOCAL_DAYS=2
BACKUP_RETENTION_REMOTE_DAYS=7

